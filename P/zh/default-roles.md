# O.2. 默认角色重命名为预定义角色

O.2. 默认角色重命名为预定义角色
版本：
纠错本页面
搜索
目录导航
❮
❯
O.2. 默认角色重命名为预定义角色 #
PostgreSQL 13 和以下版本使用术语 “Default Roles”。
但是，这些角色实际上不能被更改，并且在初始化时作为系统的一部分安装，
所以更适合的术语是 “Predefined Roles”。
当前文档关于预定义角色请参见 第 21.5 节，关于此变化的详情
请参见 PostgreSQL 14 的版本说明。
上一页 上一级 下一页O.1. recovery.conf 文件合并到 postgresql.conf 起始页 O.3. pg_xlogdump 重命名为 pg_waldump
