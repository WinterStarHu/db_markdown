# 26.1. 不同方案的比较

26.1. 不同方案的比较
版本：
纠错本页面
搜索
目录导航
❮
❯
26.1. 不同方案的比较 #共享磁盘故障转移
共享磁盘故障转移避免了只使用一份数据库拷贝带来的同步开销。它使用一个由多个服务器共享的单一磁盘阵列。如果主数据库服务器失效，后备服务器则可以挂载并启动数据库，就好像它从一次数据库崩溃中恢复过来。这允许快速的故障转移，并且不存在数据丢失。
共享硬件功能在网络存储设备中很常见。也可以使用一个网络文件系统，但是要注意的是该文件系统应具有完全的POSIX行为（见第 18.2.2.1 节）。这种方法的一个重大限制是如果共享磁盘阵列失效或损坏，主服务器和后备服务器都会变得无法工作。另一个问题是在主服务器运行时，后备服务器永远不能访问共享存储。
File System (Block Device) Replication
共享硬件功能的一种修改版本是文件系统复制，在其中对一个文件系统的所有改变会被镜像到位于另一台计算机上的一个文件系统。唯一的限制是该镜像过程必须能保证后备服务器有一份该文件系统的一致的拷贝 — 特别是对后备服务器的写入必须按照主服务器上相同的顺序进行。DRBD是用于 Linux 的一种流行的文件系统复制方案。
Write-Ahead Log Shipping
温备和热备服务器能够通过读取一个预写式日志（WAL）记录的流来保持为当前状态。如果主服务器失效，后备服务器拥有主服务器的几乎所有数据，并且能够快速地被变成新的主数据库服务器。这可以是同步的或异步的，并且只能用于整个数据库服务器。
可以使用基于文件的日志传送（第 26.2 节）、流复制（见第 26.2.5 节）或两者的组合来实现一个后备服务器。关于热备的信息可见第 26.4 节。
Logical Replication
逻辑复制允许数据库服务器发送数据更新流给另一台服务器。PostgreSQL逻辑复制从WAL构建出逻辑数据更新流。逻辑复制允许逐个表复制数据更改。此外，发布数据更新的服务器可以同时订阅其他服务器的更改，从而允许数据在多个方向流动。有关逻辑复制的更多信息，请参考 第 29 章。通过逻辑解码接口 (第 47 章)，第三方扩展也能提供类似的功能。
基于触发器的主备复制
基于触发器的复制通常会将修改数据的查询发送到指定的主服务器。它在逐个表的基础上工作，主服务器（通常）将数据更改异步发送到备用服务器。
主服务器运行时，备用服务器可以响应查询，并允许一些本地数据修改或写入操作。这种形式的复制通常用于减轻大数据分析型平台或者数据仓库查询负荷。
Slony-I是这种复制类型的一个例子。它使用表粒度，并且支持多个备用服务器。因为它会异步更新备用服务器（批量），在故障转移时可能会有数据丢失。
基于SQL的复制中间件
通过基于SQL的复制中间件，一个程序拦截每一个 SQL 查询并把它发送给一个或所有服务器。每一个服务器独立地操作。读写查询必须被发送给所有服务器，这样每一个服务器都能接收到任何修改。但只读查询可以被只发送给一个服务器，这样允许读负载在服务器之间分布。
如果查询未经修改发送，则函数的random()随机值和CURRENT_TIMESTAMP函数的当前时间和序列值可能因不同服务器而异。
因为每个服务器独立运行，并且它发送 SQL 查询而没有真正的更改数据。如果这是不可接受的，那么中间件或应用程序必须从单一源确定此类值，并将结果用于写入查询。
还必须注意确保所有服务器在提交或中止事务时都是相同的。这将涉及使用两阶段提交（PREPARE TRANSACTION和COMMIT PREPARED）。
Pgpool-II和Continuent Tungsten就是这种复制的例子。
异步多主复制
对于不会被定期连接或通信链路较慢的服务器，如笔记本或远程服务器，保持服务器间的数据一致是一个挑战。通过使用异步的多主复制，每个服务器独立工作，并且定期与其他服务器通信以识别冲突的事务。这些冲突可以由用户或冲突解决规则来解决。Bucardo 是这种复制类型的一个例子。
同步多主复制
在同步多主复制中，每个服务器可以接受写请求，并且在每个事务提交之前，修改的数据会从原始服务器传送给每个其他服务器。繁重的写活动可能导致过多的锁定和提交延迟，进而导致很差的性能。读请求可以被发送给任意服务器。某些实现使用共享磁盘来减少通信负荷。同步多主复制主要适用于读负载，尽管它的大优点是任意服务器都能接受写请求 — 没有必要在主服务器和备用服务器之间划分负载，并且因为数据修改被从一个服务器发送到另一个服务器，不会有非确定函数（如random()）的问题。
PostgreSQL不提供这种复制类型，尽管在应用代码或中间件中可以使用PostgreSQL的两阶段提交（PREPARE TRANSACTION和COMMIT PREPARED）来实现这种复制。
表 26.1总结了上述多种方案的能力。
表 26.1. 高可用性、负载均衡和复制特性矩阵特性共享磁盘文件系统复制预写式日志传送逻辑复制基于触发器的复制SQL复制中间件异步多主复制同步多主复制常用的示例NASDRBD内建流复制内建逻辑复制，pglogicalLondiste，Slonypgpool-IIBucardo 通信方法共享磁盘磁盘块WAL逻辑解码表行SQL表行表行和行锁不要求特殊硬件 •••••••允许多个主服务器   • •••无主服务器负载• •• •  不等待多个服务器• with sync offwith sync off• • 主服务器失效将永不丢失数据••with sync onwith sync on • •复制体接受只读查询  with hot standby•••••每个表粒度   •• ••不需要冲突解决••• •• •
有一些解决方案不适合上述类别：
数据分区
数据分区将表拆分为数据集。每个集合只能被一个服务器修改。例如，数据可以根据办公室进行分区，例如伦敦和巴黎，每个办公室有一个服务器。如果需要组合伦敦和巴黎的数据，可以通过应用程序查询两个服务器，或者可以使用主/备复制在每台服务器上保持其他办公室数据的只读副本。
多服务器并行查询执行
上述的许多方案允许多个服务器处理多个查询，但没有一个方案允许单个查询使用多个服务器以更快完成。该方案允许多个服务器在单个查询上并发工作。通常通过将数据在服务器之间拆分，让每个服务器执行其查询部分并将结果返回给中心服务器，在那里将结果合并并返回给用户。这可以使用PL/Proxy工具集来实现。
还需要注意的是，由于PostgreSQL是开源的并且易于扩展，许多公司已经采用PostgreSQL并创建了具有独特故障转移、复制和负载平衡能力的商业闭源解决方案。这里不讨论这些。
上一页 上一级 下一页第 26 章 高可用性、负载均衡和复制 起始页 26.2. 日志传送后备服务器
