# DROP USER

DROP USER
版本：
纠错本页面
搜索
目录导航
❮
❯
DROP USERDROP USER — 移除数据库角色大纲
DROP USER [ IF EXISTS ] name [, ...]
说明
DROP USER是DROP ROLE的另一种写法。
兼容性
DROP USER语句是一个PostgreSQL扩展。SQL标准将用户的定义留给具体实现。
另见DROP ROLE上一页 上一级 下一页DROP TYPE 起始页 DROP USER MAPPING
