# 18.8. 加密选项

18.8. 加密选项
版本：
纠错本页面
搜索
目录导航
❮
❯
18.8. 加密选项 #
PostgreSQL提供了几个不同级别的加密，并且在保护数据不会因为数据库服务器偷窃、不道德的管理员、不安全网络等因素而泄漏方面提供很高的灵活性。加密可能也是保护一些诸如医疗记录或财务交易等敏感数据所要求的。
密码加密
数据库用户的密码都是以哈希(取决于password_encryption配置)的方式存储，所以管理员不能确定实际的密码赋予用户。如果 SCRAM 或者 MD5 加密算法被用于客户端认证，那么未加密的密码甚至都不可能出现在服务器上，因为客户端在通过网络发送密码之前，就已经加密过。推荐使用SCRAM，因为它是互联网标准而且相比于PostgreSQL特定的MD5认证协议更安全。
警告
对 MD5 加密密码的支持已被弃用，并将在未来的 PostgreSQL 版本中移除。
有关迁移到其他密码类型的详细信息，请参阅 第 20.5 节。
特定列加密
pgcrypto模块允许对某些字段进行加密存储。这个功能只对某些敏感数据有用。客户端提供解密的密钥，然后数据在服务器端解密并发送给客户端。
在数据被解密和在服务器与客户端之间传递时，解密数据和解密密钥将会在服务器端存在短暂的一段时间。这就给那些能完全访问数据库服务器的人提供了一个短暂的截获密钥和数据的时间，例如系统管理员。
Data Partition Encryption
存储加密可以在文件系统层面或者块层面上执行。Linux 文件系统加密
选项包括 eCryptfs 和 EncFS，而 FreeBSD 使用 PEFS。块层面或者全
盘加密选项包括 Linux 上的 dm-crypt + LUKS 以及 FreeBSD 上的
GEOM 模块 geli 及 gbde。许多其他操作系统也支持这个功能，包括
Windows。
这个机制避免了在整个计算机或者驱动器被盗的情况下，未加密的数据被从驱动器中读取。它无法防止在文件系统被挂载时的攻击，因为在挂载之后，操作系统提供数据的未加密视图。不过，要想挂载该文件系统，你需要有一些方法把加密密钥传递给操作系统，并且有时候这个密钥就存储在挂载该磁盘的主机上的某处。
Encrypting Data Across A Network
SSL 连接加密所有跨网络发送的数据：密码、查询以及返回的数据。pg_hba.conf 文件允许管理员指定哪些主机可以使用非加密连接（host），以及哪些主机需要使用 SSL 加密的连接（hostssl）。客户端还可以指定它们只通过 SSL 连接到服务器。
GSSAPI 加密的连接加密通过网络发送的所有数据，包括查询和返回的数据。（不会在网络中发送密码。）pg_hba.conf 文件允许管理员指定哪些主机可以使用非加密连接（host）和哪些主机需要 GSSAPI 加密连接（hostgssenc）。此外，客户端还可以指定它们仅在 GSSAPI 加密的连接上连接服务器（gssencmode=require）。
Stunnel 或 SSH 也可以用于加密传输。
SSL Host Authentication
客户端和服务器都可以相互提供 SSL 证书。这在两边都需要一些额外的配置，但是这种方式提供了比仅使用密码更强的身份验证。它防止计算机伪装成服务器，只要足够长的时间读取客户端发送的密码。它还帮助防止 “中间人” 攻击，在这种攻击中，一台计算机位于客户端和服务器之间，伪装成服务器并读取和传递客户端与服务器之间的所有数据。
客户端加密
如果服务器所在机器的系统管理员不可信，客户端就有必要加密数据；这样，未加密的数据就不会出现在数据库服务器上。数据在发送到服务器之前会在客户端上加密，而数据库结果在使用之前必须在客户端上解密。
上一页 上一级 下一页18.7. 阻止服务器欺骗 起始页 18.9. 用 SSL 进行安全的 TCP/IP 连接
