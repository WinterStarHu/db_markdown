# 70.3. 备份清单 WAL 范围对象

70.3. 备份清单 WAL 范围对象
版本：
纠错本页面
搜索
目录导航
❮
❯
70.3. 备份清单 WAL 范围对象 #
描述 WAL 范围的对象始终有三个键：
Timeline
此 WAL 记录范围的时间线，为整数。
Start-LSN
此 LSN 位于必须在指示的时间线上开始的回放，以便使用此备份。
此 LSN 通常使用PostgreSQL的格式存储；也就是说，它是一个字符串，由两个十六进制字符字符串组成，每个字符串的长度介于 1 和 8 之间，用斜杠分隔。
End-LSN
当使用此备份时，在指示的时间线上回放的最早 LSN 可能会结束。
其存储的格式与 Start-LSN相同。
一般情况下，只有一个 WAL 范围。
但是，如果备份来自备用服务器，在备份期间由于上游升级而切换时间线，则可能存在多个范围，每个具有不同的时间线。
对于同一时间线，永远不会有多个 WAL 范围出现。
上一页 上一级 下一页70.2. 备份清单文件对象 起始页 部分 VIII. 附录
